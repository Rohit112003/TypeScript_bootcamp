//Enums allow developers to define a collection of related values with more readable and
// semantic code. Enums are helpful when you have a fixed set of values that a variable can take.

enum Color {
    Red,
    Green,
    Blue
}

// Using the enum
let backgroundColor: Color = Color.Blue;
console.log(backgroundColor);

export {}