const superHero: string[] = [];
const superPower : number[] = [];


superHero.push("spiderMan");
superPower.push(123);

type User= {
    name:string;
    isActive:boolean;
}

const manUsers: User[] = [];

manUsers.push({name:"Rohit", isActive:true})